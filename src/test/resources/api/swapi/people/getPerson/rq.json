{
  "query": "query GetPerson($id: ID!) { person(id: $id) { id name birthYear eyeColor gender hairColor height mass skinColor homeworld { name } filmConnection { films { id title episodeID } } species { name } starshipConnection { starships { name model consumables } } } }",
  "variables": {
    "id": "${id}"
  }
}